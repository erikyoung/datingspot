<br />

<div class="booyah-box col-xs-10 col-xs-offset-1">
<h1 class="text-center">Learn Something Today</h1>

  <% @advices.each do |advice| %>
    
      <div class="text-center">
        <h2><%= advice.title %></h2>
        <h6><%= advice.body %></h6>
      </div>

      <div class="text-center">
        <%= link_to "READ MORE", advice_path(advice) %>
      </div>
      <br>
    <% end %>
    
  </div>

